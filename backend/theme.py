# -*- coding: utf-8 -*-
"""
Единый источник палитры и шрифтов для альманаха.

Зачем отдельный модуль: цвета встречаются и в CSS (render.py), и внутри
генерируемых SVG (charts.py). Пока они были захардкожены в двух местах,
смена темы неизбежно оставляла диаграммы старого цвета — SVG не наследует
CSS-переменные документа.

Тема — светлая, печатная: документ рассчитан на чтение с экрана и на экспорт
в PDF/A4, поэтому фон бумажный, а не залитый краской, и все акценты
достаточно тёмные, чтобы читаться при печати в оттенках серого.
"""

# ─── Поверхности ──────────────────────────────────────────────────────────────
PAPER   = "#eef0ec"          # фон страницы
PANEL   = "#ffffff"          # карточки, таблицы
PANEL2  = "#f6f8f4"          # чётные строки таблиц, мягкая заливка

# ─── Акцент ───────────────────────────────────────────────────────────────────
ACCENT  = "#6e5624"          # основной (заголовки, номера секций, шапки таблиц)
ACCENT2 = "#6b5423"          # вторичный акцент
ACCENT_WASH = "rgba(138,109,47,.08)"   # едва заметная заливка акцентом
ACCENT_SOFT = "rgba(138,109,47,.16)"   # подсветка текущей строки

# ─── Текст ────────────────────────────────────────────────────────────────────
INK     = "#252a22"          # основной текст
MUTED   = "#525a4e"          # вторичный
FAINT   = "#5c6356"          # третичный, подписи

# ─── Линии ────────────────────────────────────────────────────────────────────
LINE      = "rgba(138,109,47,.28)"
LINE_SOFT = "rgba(138,109,47,.18)"

# ─── Семантика (годится и для ч/б печати: разная светлота) ────────────────────
GREEN   = "#2f6340"          # сила, реализовано
BLUE    = "#2e567c"          # спасено игроком
YELLOW  = "#7f6517"          # потенциал без ключа
RED     = "#8c3838"          # уязвимость
SATURN  = "#4a5f96"

# Оттенки для сетки достоинств — на белом нужен запас контраста
DIGNITY_COLORS = {
    "Э":  GREEN, "МТ": GREEN, "С": "#3d7a52",
    "д":  BLUE,  "н":  MUTED, "в": "#8a6d10", "П": RED,
}

# ─── Шрифты ───────────────────────────────────────────────────────────────────
# Локальные засечные вместо Google Fonts: CDN недоступен офлайн и не
# резолвится в PDF-рендерерах, из-за чего документ печатался дефолтным
# шрифтом. Georgia/Times дают приемлемый фолбэк на Windows и macOS.
FONT_SERIF   = "'DejaVu Serif',Georgia,'Times New Roman',serif"
FONT_DISPLAY = "'FreeSerif','DejaVu Serif',Georgia,serif"
FONT_MONO    = "'DejaVu Sans Mono',ui-monospace,'Courier New',monospace"
